/*
import 'package:database_project/components/ds_project_display_title.dart';
import 'package:flutter/material.dart';

import '../components/DS_app_bar.dart';
import 'employe_project_view.dart';

class EmployeeHome extends StatelessWidget {
  const EmployeeHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: const DSAppBar(
          title: 'My Projects',
        ),
        body: Padding(
          padding: const EdgeInsets.all(12.0),
          child: Column(
            children: [
              DSProjectDisplayTitle(
                onTap: () {
                  Navigator.push(
                    context,
                    MaterialPageRoute(
                        builder: (context) => const EmployeeProjectView()),
                  );
                }, projectTitle: '', dueDate: DateTime(2024, 12, 20)
              ),
            ],
          ),
        ),
      ),
    );
  }
}
*/
